[COMBAT]
disable_damage_mod_msg=0  # Disable damage immunity/reduction/
                          # resistance messages.

disable_dev_critical=0    # Disable Devistating Critical death.

disable_circle_kick=0     # Disable circle kick attacks.

disable_parry=0           # Disable parry attacks. Note:
                          # The mode can still be activated, it
                          # will simply have no affect.